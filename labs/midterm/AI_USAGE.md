# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** ภารดี อ่อนละออ **รหัส:** 68543210037-6 **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 12.28 | B1.1 | แจ้งเตือน Warning สีเหลืองเรื่อง unique key prop ในลิสต์ เกิดจากอะไร | แนะนำเรื่อง Virtual DOM Reconciliation และระบุว่าการใช้ .map() ต้องใส่ prop key ให้ Root Element | ตรวจสอบ src/components/RequestList.jsx และใส่ key={request.id} ที่ <RequestCard/> |
| 12.51 | B1.4 | เปลี่ยน URL parameter จาก REQ-001 เป็น REQ-002 แต่หน้าเว็บไม่ดึงข้อมูลใหม่มาแสดง | แนะนำเรื่อง Dependency Array ของ useEffect ว่าต้องใส่ตัวแปรที่ต้องการให้รันฟังก์ชันซ้ำ | เพิ่ม requestId ลงใน Dependency Array ใน RequestDetailPage.jsx เป็น [requestId, reloadKey] · หน้ารายละเอียดอัปเดตตาม URL ทันที |
| 13.09 | B1.6 | กดปุ่ม Reset Demo Data แล้วขึ้นหน้าขาว/หน้าพัง เกิดจากอะไร | แนะนำว่าฟังก์ชันที่เป็น Asynchronous คืนค่าเป็น Promise หากไม่รอผลลัพธ์ State จะได้ค่าที่ไม่ใช่ Array | ปรับฟังก์ชันใน DashboardPage.jsx เป็น async และใส่ await ด้านหน้า resetRequests() ก่อนนำผลลัพธ์ไป setRequests · ข้อมูลรีเซ็ตได้ปกติ |
| 13.20 | B2.1 | วิธีสร้าง Search Input สำหรับรับคำค้นหาใน React | แนะนำการสร้าง State สำหรับเก็บคำค้นหา และผูกค่าผ่าน value กับ onChange event | สร้างช่อง Input ค้นหาใน DashboardPage.jsx พร้อมผูก State เพื่อเก็บค่าคำค้นหาแบบ Real-time |
| 13.28 | B2.2 | การทำระบบ Search ให้ค้นหาข้อความได้ทั้ง requesterName และ details แบบไม่สนตัวพิมพ์เล็ก-ใหญ่ | แนะนำการใช้ .toLowerCase() และ .includes() กับทั้งสองฟิลด์ในการกรอง Array | เขียนเงื่อนไขใน DashboardPage.jsx · ทดสอบพิมพ์คำค้นหาแล้วพบข้อมูลที่ตรงกันอย่างถูกต้อง |
| 13.35 | B2.3 | การรวมเงื่อนไข Filter สถานะและระบบ Search เข้าด้วยกันใน React | แนะนำการทำ Derived State กรองทั้ง filterStatus และ searchKeyword ร่วมกันในขั้นตอนเดียว | นำเงื่อนไขมาผสานกันใน DashboardPage.jsx · ทดสอบเลือกสถานะพร้อมพิมพ์คำค้นหา รายการแสดงผลตรงตามเงื่อนไขทั้งสอง |
| 13.42 | B2.4 | วิธีทำ Empty State เมื่อผลการค้นหาไม่พบรายการใดเลย | แนะนำการเช็กความยาวของ Array ผลลัพธ์ (length === 0) เพื่อแสดงข้อความแจ้งเตือนผู้ใช้ | เพิ่มการแสดงข้อความแจ้งเตือนใน DashboardPage.jsx เมื่อค้นหาไม่พบข้อมูล · หน้าจอแสดงข้อความตามที่โจทย์กำหนด |
| 13.50 | B3.1 | วิธีเพิ่มปุ่ม ทำเสร็จ ใน RequestCard ให้แสดงเฉพาะรายการที่ยังไม่ completed | แนะนำการใช้ Conditional Rendering (status !== 'completed') และการส่ง callback prop onMarkDone | เขียนปุ่มใน RequestCard.jsx พร้อมกำหนดเงื่อนไขและคลาสปุ่ม · ปุ่มแสดงเฉพาะการ์ดที่สถานะยังไม่เสร็จสมบูรณ์ |
| 14.08 | B3.2 | กดปุ่มทำเสร็จแล้ว ต้องการให้สถานะเปลี่ยนและแผงสรุปอัปเดตตัวเลขทันที | แนะนำให้เรียกใช้ updateRequestStatus แล้วนำผลลัพธ์ Array ชุดใหม่มาอัปเดต State ผ่าน setRequests | เขียนฟังก์ชัน handleMarkDone ใน DashboardPage.jsx และส่งเป็น prop ไปยัง Card| Local Storage พบ Key บันทึกข้อมูลถูกต้อง |
| 14.18 | B4.1 | แนวทางการสร้าง Component PriorityBadge เพื่อแยกแสดงป้าย เร่งด่วน และ ปกติ | แนะนำการเขียน Functional Component รับ prop priority และใช้เงื่อนไขตรวจสอบค่าเพื่อคืนค่า span พร้อม class ที่กำหนด | สร้างไฟล์ src/components/PriorityBadge.jsx ตรวจสอบเงื่อนไข urgent และ normal พร้อมกำหนด className ให้ตรงตามข้อกำหนด |
| 14.23 | B4.2 | ทำไมต้องดักจับ Edge Case กรณีค่า priority เป็นค่าอื่น และควรทดสอบอย่างไร | แนะนำเรื่อง Defensive Programming เพื่อป้องกัน UI พังเมื่อได้ค่าผิดปกติ และแนะนำให้ลองส่งค่าจำลองมาทดสอบก่อนลบออก | เพิ่มเงื่อนไขคืนค่า "ไม่ระบุ" พร้อม class priority-unknown และทดสอบใส่ <PriorityBadge priority="high"/> ชั่วคราวเพื่อยืนยันผลก่อนลบออก |
| 14.38 | B4.3 | การนำ PriorityBadge ไปใช้งานแทนข้อความเดิมใน RequestCard | แนะนำให้ Import Component เข้ามาและส่ง request.priority เข้าไปเป็น prop แทนที่ตำแหน่งเดิม | แก้ไขไฟล์ RequestCard.jsx โดยเรียกใช้ <PriorityBadge priority={request.priority}/> แทนการแสดงผลข้อความเดิม · การ์ดแสดงป้ายสถานะถูกต้อง |

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ภารดี อ่อนละออ
